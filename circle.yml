# Specify of branches to build
general:
  branches:
    only:
      - development
      - master

machine:
  node:
    version: 9.3.0

deployment:
  staging:
    branch: development
    heroku:
      appname: flagis-web-development

#  production:
#    branch: master
#    heroku:
#      appname: flagis-web-staging
