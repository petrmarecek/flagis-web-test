import styled from 'styled-components'

import IconComponent from '../../icons/icon'

const Button = styled(IconComponent).attrs({
  color: ['#B1B5B8'],
  hoverColor: ['#293034'],
  width: 24,
  height: 27,
  scale: 0.926,
})`
  margin-left: 10px;
`

export {
  Button,
}
